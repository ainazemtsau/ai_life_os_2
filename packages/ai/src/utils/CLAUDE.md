# Utils

Utilities for context management and error handling.

## Index

| File                      | Contents (WHAT)                                          | Read When (WHEN)                                |
| ------------------------- | -------------------------------------------------------- | ----------------------------------------------- |
| `context-truncation.ts`   | Token counting, message truncation preserving context    | Debugging token limits, context window issues   |
| `retry.ts`                | Exponential backoff retry wrapper                        | Adding retry logic, handling API failures       |
| `observability.ts`        | Token tracking, error logging for LLM workflows          | Implementing usage metrics, monitoring LLM calls |
